defaults:
  - model: vitg14_reg4
  - ddp: fsdp
  - optim: adamw
  - train: vitg14_reg4
  - logging: wandb
  - _self_

seed: 0
base_path: /nfs/scratch2/shared_image_recog_ml
weights_path: ${base_path}/weights/dinov2
TARGET_FILE_EXTENSION: ".pth"
search_suffix: "2D/**/*${TARGET_FILE_EXTENSION}"
max_files: 100000

logging:
  use_wandb: true
  wandb_config:
    name: heads_pretrain
  log_preds: true
  log_pred_steps: 200
  num_images: 5

train:
  dataset_path: ${base_path}/care_llsm_518
  path_to_txt: ${train.dataset_path}/train.txt
  output_dir: ${weights_path}/care_llsm_518
  search_suffix: ${search_suffix}
  max_files: ${max_files}
  num_workers: 150

test:
  dataset_path: ${base_path}/llsm_full_res
  path_to_txt: ${test.dataset_path}/test.txt
  search_suffix: ${search_suffix}
  max_files: ${max_files}

model:
  weights: ${weights_path}/pretrained/dinov2_vitg14_reg4_pretrain_ckpt.pth

evaluation:
  eval_period_steps: 1500
