train_dataset:
  _target_: cell_interactome.data.dino3d.dataset.make_webdataset
  base_data_dir: ""
  batch_size: 8
  shuffle_buffer_size: 750
  nodesplitter:
    _partial_: true
    _target_: webdataset.split_by_node

test_dataset:
  _target_: cell_interactome.data.dino3d.dataset.make_webdataset
  base_data_dir: ""
  batch_size: ${logging.num_pca_images}
  shuffle_buffer_size: 0
  nodesplitter:
    _partial_: true
    _target_: webdataset.split_by_worker
