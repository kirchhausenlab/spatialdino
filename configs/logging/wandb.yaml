experiment_logger:
  _target_: cell_interactome.logging.experiment_loggers.WandbExperimentLogger
  project: cell-interactome
  entity: artificial-intelligence-research
  name: ${experiment_name}
  resume: false

log_interval: 10
eval_interval: 1000
pred_interval: 10
num_pred_images: 5
